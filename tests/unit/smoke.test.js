import { describe, it, expect } from 'vitest';

// Validates the Vitest scaffolding itself. Real suites live alongside this.
describe('vitest scaffolding', () => {
  it('runs arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
